coverage:
  precision: 2
  round: up
  range: "50...75"
  status:
    patch:
      default:
        thresold: null
    project:
      default:
        threshold: 3
      client:
        flags: client
      daemon:
        flags: daemon

flags:
  client:
    paths:
      - cfg/
      - cmd/
      - client/
      - local/
      - provision/
      - common/
  daemon:
    paths:
      - daemon/inertiad/
      - common/
  #web:
  #  paths:
  #    - daemon/web
